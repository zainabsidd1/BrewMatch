# Local Next.js frontend. Production values live in the Vercel dashboard.
# Set this to your Render API URL in Vercel (no trailing slash), e.g.
# https://brewmatch-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8000
